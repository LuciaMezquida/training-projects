/*Crea un programa que defina un array de 5 nombres y luego 
utilice un bucle para mostrar los nombres de uno en uno*/

const names = ['Muka', 'Mora', 'Sombrita', 'Mina', 'Coco'];

/*for(i = 0; i < names.length; i++) {
    console.log(names[i]);
}
*/

/*for(i in names) {
    console.log(names[i]);
}
*/

for(list of names) {
    console.log(list);
}